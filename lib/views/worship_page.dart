import 'package:flutter/material.dart';

class WorshipPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Worhip Page'),
      ),
      body: Center(
        child: Text('Worhip Page Content'),
      ),
    );
  }
}
